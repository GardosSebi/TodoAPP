# Copy to .env and set per environment (dev/prod).
# Used by docker compose for app env vars. Do not commit .env.

# Deployment version (set by CI/CD)
GIT_SHA=unknown

# Optional: app environment label (dev, staging, prod)
APP_ENV=dev
